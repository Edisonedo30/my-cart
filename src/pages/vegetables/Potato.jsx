import React from 'react'

export const Potato = () => {
  return (
    <div>provide a valid href, but still need the element to resemble a link, use a button and change it with appropriate styles.</div>
  )
}
